*.pyc
.sconsign.dblite
*.os
**/*.o
*.obj

# Toolchain local de frt_env.sh (SDK buildroot + SDL2 arm64)
/.frt-env/
# Fuentes del runtime TheGates de thegates_env.sh (modulo the_gates + thirdparty)
/.thegates-env/

# Preview generado del demo de glow map (el nightly lo adjunta al release)
/demos/glow_map/media/
